# Codex Review — Flow Feature Update 2026-05 PRD + Discovery

## Spec-compliance
- [PASS] LP removal intent covered: Discovery says LP labels are absent and stale `MODEL_MAP` entries will fail (`docs/FLOW_DISCOVERY_2026-05-20.md:36`, `docs/FLOW_DISCOVERY_2026-05-20.md:49`); PRD assigns purge plus old-alias compatibility to Unit A (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:29`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:67`).
- [PASS] Agent-off intent covered: Discovery says Agent hides normal composer controls and should be toggled OFF (`docs/FLOW_DISCOVERY_2026-05-20.md:105`, `docs/FLOW_DISCOVERY_2026-05-20.md:115`); PRD assigns reverseAPI disable to Unit C (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:38`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:116`).
- [PASS] Free-tier L2 paywall intent covered: Discovery records paid-subscriber banner and missing legacy L2 buttons (`docs/FLOW_DISCOVERY_2026-05-20.md:79`, `docs/FLOW_DISCOVERY_2026-05-20.md:88`); PRD assigns clean `paid_tier_required` fail to Unit D (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:39`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:144`).
- [GAP] Duration post-probe not incorporated: Discovery follow-up says duration is deferred and Unit B AC#6/#7 should strike `duration` plumbing (`docs/FLOW_DISCOVERY_2026-05-20.md:301`); PRD still scopes `duration`, schema, migration, risk, and default answer (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:37`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:99`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:170`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:183`).
- [GAP] Credit preview guard dropped: Discovery captures `Generating will use N credits` and x4 -> 40-credit preview (`docs/FLOW_DISCOVERY_2026-05-20.md:32`, `docs/FLOW_DISCOVERY_2026-05-20.md:71`, `docs/FLOW_DISCOVERY_2026-05-20.md:254`); PRD only asks to re-verify cost, with no pre-submit guard AC (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:227`).
- [GAP] Asset-picker existing-media attach dropped: Discovery says `+` can attach existing Images/Videos/Voices/Characters and unlock chained generation without re-upload (`docs/FLOW_DISCOVERY_2026-05-20.md:140`, `docs/FLOW_DISCOVERY_2026-05-20.md:156`); PRD defers voice only, not existing project media attach (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:44`).
- [GAP] Voice-ingredients scope contradicts itself: PRD goal promises cleanup of voice ingredients (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:13`), then defers voice asset attach (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:44`).
- [GAP] Image catch-up is over-scoped against current code: Discovery says image models are never explicitly selected (`docs/FLOW_DISCOVERY_2026-05-20.md:191`, `docs/FLOW_DISCOVERY_2026-05-20.md:197`), but current `flow/operations/image.py` already has image model and 5-ratio maps and calls them (`flow/operations/image.py:58`, `flow/operations/image.py:71`, `flow/operations/image.py:341`).
- [GAP] P1 scope label inaccurate: PRD says `P0 + P1` in scope (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:25`) but defers Trash/Share/Characters/Scenes/Tools (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:45`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:46`). This may be intentional, but should be called “selected P0/P1”.
- [GAP] Project auto-name dropped silently: Discovery notes new human-readable project names and optional dashboard field (`docs/FLOW_DISCOVERY_2026-05-20.md:174`, `docs/FLOW_DISCOVERY_2026-05-20.md:178`); PRD has no explicit defer or docs-only note.

## Findings (severity-tagged)
### Critical
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:99`: Unit B still requires `duration` passthrough after Discovery explicitly deferred it | implementer must invent API/UI behavior not observed, risking schema churn and paid-credit live probing | remove all `duration` AC/migration/open-answer text until paid-tier selector/payload is captured.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:191`: “All 4 units are file-disjoint” is false | fan-out workers will conflict or miss required files | split schema work into one unit or add explicit shared-file serialization; Unit B owns `server/models/job.py` (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:82`) while Unit D needs `error_kind` schema (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:171`), Unit C says edit `flow/client.py` while forbidding all other files (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:111`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:113`).
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:123`: Unit C names `FlowClient.navigate_to_project()`, but `FlowClient` exposes lifecycle/reset methods, not that hook (`flow/client.py:540`, `flow/client.py:629`) | Agent OFF code has no real invocation point | choose actual call sites in L1/L2 navigation or add a central navigation helper and give Unit C ownership.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:121`: reverseAPI fetch is described as same-origin/auto-auth, but captured endpoint is `aisandbox-pa.googleapis.com` while Flow page is `labs.google` (`docs/FLOW_DISCOVERY_2026-05-20.md:110`) | CORS/auth/body may fail even if DOM detection works | capture exact request body, headers, credentials mode, and response before coding; do not rely on implicit same-origin auth.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:148`: `error_kind`/`error_message` persistence is not supported by current server model/store | Unit D AC can pass locally but server update drops or rejects fields, leaving only `error` visible | add owned changes to `server/models/job.py`, `server/db/database.py`, and `server/db/job_store.py`, or encode paywall kind in existing `error`; current `JobUpdate` has no `error_kind`/`error_message` (`server/models/job.py:274`) and DB has only `error` (`server/db/database.py:68`).
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:148`: Unit D dispatcher catch is insufficient for batch/multitab L2 | paywall exceptions raised inside tab workers become generic failed results, not `paid_tier_required`, and dispatcher never sees them | add explicit `L2PaywallError` propagation/handling in `_multitab.py`, `_batch.py`, and batch dispatcher; `_multitab.py` currently catches generic exceptions and returns `error: str(exc)` (`flow/operations/_multitab.py:366`, `flow/operations/_multitab.py:371`), while top-level L2 batches route to multitab (`worker/dispatcher.py:1309`).
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:98`: Unit B says add `_set_output_count()` to extend/insert/remove/camera, but Unit B only owns `generate.py` and forbids `_base.py` (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:80`, `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:91`) | AC is unimplementable in assigned write set | either move L2 count/paywall work to Unit D or remove L2 count from Unit B until paid-tier L2 composer is observed.

### Important
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:132`: Unit D needs `flow.exceptions.L2PaywallError`, but `flow/exceptions.py` is not in the stated OWNS list and repo patterns keep exceptions near owning modules (`flow/recaptcha.py:190`, `flow/operations/_base.py:27`) | worker may create an unowned shared file or inconsistent exception location | add `flow/exceptions.py` to Unit D OWNS or define `L2PaywallError` in `_base.py` and import from there.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:83`: Unit B references `server/db/schema.sql`, but current schema/migrations live in `server/db/database.py` (`server/db/database.py:35`, `server/db/database.py:333`) | migration AC points workers at a non-existent file | replace with `server/db/database.py` ownership if any schema field remains.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:57`: test paths are wrong for current repo layout | workers may create parallel `tests/flow/...` tree instead of updating existing tests | use current root tests, e.g. `tests/test_model_selector.py:1`, `tests/test_generate.py:1`, and existing dispatcher tests under `tests/`.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:68`: adding image models to `MODEL_MAP` alone does not affect text-to-image | current image flow uses local `IMAGE_MODEL_MAP` and `_select_image_model()` (`flow/operations/image.py:58`, `flow/operations/image.py:341`) | either migrate `image.py` to shared registry under Unit A ownership or leave image registry local and narrow Unit A.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:70`: alias compatibility is mandatory because old LP defaults still exist outside Unit A | stored jobs and dispatcher defaults can still pass `veo-3.1-*-lp` | keep aliases until server/worker defaults migrate; current defaults include `server/models/job.py:23`, `worker/dispatcher.py:257`, `worker/dispatcher.py:290`, `worker/dispatcher.py:361`, `worker/dispatcher.py:393`.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:145`: “absence of legacy L2 buttons” is unsafe as a paywall signal | paid timeline UI may also remove old buttons while still supporting timeline `+` | make banner/Upgrade CTA the only positive paywall signal; treat missing buttons as diagnostic context.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:148`: paywall error kind is inconsistent | AC says `error_kind = "paid_tier_required"`, live verify expects `paid_tier_required_extend` (`docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:154`) | choose one normalized shape, e.g. `error_kind="paid_tier_required"` plus `op="extend-video"`, or encode full kind in `error`.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:120`: Agent disable mutates persistent project state | Discovery says toggle persists across reloads (`docs/FLOW_DISCOVERY_2026-05-20.md:110`) | disable only immediately before deterministic automation, log mutation, and consider restoring prior state if user-facing projects matter.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:71`: chip icon AC over-specifies unverified icon names | Discovery says image default icon is “crop_square or similar” and mentions banana/model chip variants, not exact 4:3/3:4 icon ligatures (`docs/FLOW_DISCOVERY_2026-05-20.md:74`) | prefer robust role/text/menu discovery over exact Material icon inventory.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:163`: integration smoke test is cross-cutting but not owned by any unit | fan-out merge may leave integration test uncreated or cause late conflicts | assign integration test to a final reconciliation PR, not a parallel unit.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:103`: Unit B `xhigh` is justified, but Unit C `high` is too low | CLAUDE §4 treats profile/account/recovery paths as critical invariants (`CLAUDE.md:67`, `CLAUDE.md:98`) and Unit C touches per-project persistent state plus reverseAPI auth | mark Unit C `xhigh` or reduce it to DOM-only no-op detection.

### Minor / Nit
- `docs/FLOW_DISCOVERY_2026-05-20.md:5`: Discovery says “5 changelogs since” but table lists 4 dated rows (`docs/FLOW_DISCOVERY_2026-05-20.md:15`) | minor trust friction | fix count or add missing 5/5 row.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:25`: “P0 + P1” label conflicts with explicit deferrals | reviewers may treat deferred P1 as missed implementation | rename to “selected P0/P1 plus selected pre-baseline gaps”.
- `docs/PRD_FLOW_FEATURE_UPDATE_2026-05.md:231`: default `nano-banana-pro` may be costlier than `nano-banana-2` and cost is unverified | free-tier worker may burn credits unexpectedly | make image default a config decision after preview capture.
- `docs/FLOW_DISCOVERY_2026-05-20.md:305`: “Return silent videos” dismissed as irrelevant, but setting may affect output audio/cost | product behavior unknown | keep as follow-up probe, not a hard irrelevant claim.

## Unit-by-unit Verdict
- Unit A: REQUEST_CHANGES — good LP/alias target, but image registry is already local in `image.py` and server/worker LP defaults remain outside ownership.
- Unit B: REQUEST_CHANGES — `xhigh` justified, but duration must be removed, L2 count AC conflicts with write set, and actual Frames/Ingredients helpers live outside `generate.py`.
- Unit C: REQUEST_CHANGES — call site is nonexistent, `flow/client.py` edit violates ownership, endpoint body/auth are unverified, and reasoning level should be `xhigh` if reverseAPI remains.
- Unit D: REQUEST_CHANGES — `xhigh` justified, but schema/exception ownership and batch/multitab propagation are missing; current AC cannot guarantee no burn/no retry across all L2 paths.

## False-assumption flags
- “LP deprecation has happened” (`docs/FLOW_DISCOVERY_2026-05-20.md:46`) | one free-tier account dropdown; LP may remain on paid/legacy/rollout cohorts | verify same dropdown on ngoctuandt20, one paid account, and one second free account.
- “Lite = 10 credits each now” (`docs/FLOW_DISCOVERY_2026-05-20.md:52`) | derived from `40 credits / x4`; cost could vary by duration, count bundle, or account state | set x1 and read preview; optionally burn one controlled Lite x1 submit with capture.
- “Default Image is now 1:1” (`docs/FLOW_DISCOVERY_2026-05-20.md:67`) | composer mode/aspect may be sticky per account/session | clear site storage or use fresh profile and open new project twice.
- “Default app mode is now Image with Nano Banana Pro” (`docs/FLOW_DISCOVERY_2026-05-20.md:77`) | previous Image run can persist mode/model | verify on fresh profile, fresh project, and after toggling Video then reload.
- “L2 ops are fully paywalled for free tier” (`docs/FLOW_DISCOVERY_2026-05-20.md:232`) | evidence is one free account; could be experiment, cooldown, or account entitlement | test ngoctuandt20, second free account, and paid account; capture DOM plus network.
- “Default video duration ~8s” (`docs/FLOW_DISCOVERY_2026-05-20.md:96`) | one existing generation/player timeline does not prove default for new submits | generate or inspect fresh x1 preview/metadata; search paid UI for 4/6/8 selector.
- “No duration selector anywhere in free-tier composer” (`docs/FLOW_DISCOVERY_2026-05-20.md:284`) | viewport/account/feature flag may hide control | run DOM text search for `4s|6s|8s|duration` across free and paid profiles.
- “Characters uses Nano Banana 2” (`docs/FLOW_DISCOVERY_2026-05-20.md:266`) | one page default can be sticky or feature-flagged | open Characters on fresh project/profile and inspect model menu.
- “Scenes is dead route” (`docs/FLOW_DISCOVERY_2026-05-20.md:269`) | 404 may be account/region rollout | retry on paid/other free account before hard-defer rationale relies on it.
- “No hidden Insert/Remove/Camera options” (`docs/FLOW_DISCOVERY_2026-05-20.md:232`) | only top-bar kebab checked; timeline/context/keyboard surfaces remain possible | right-click/timeline `+`/keyboard probe on paid account with zero-credit navigation first.

## Recommendation for next step
- Not ready to fan-out. Blockers: strike duration, repair unit ownership, define real Agent call/auth path, redesign Unit D schema plus batch propagation, and reconcile image/model scope with existing `image.py`.
- After PRD patch, run one shared live probe on ngoctuandt20 plus one paid profile if available; do not let each Codex unit independently infer missing Flow API shapes.
